---
- name: Failing Playbook for Retry Tests
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: This task always fails
      ansible.builtin.command: /bin/false
      changed_when: false
