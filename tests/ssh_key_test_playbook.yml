---
- name: SSH Key Format Validation Test
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
      - name: Check if private key was processed
        ansible.builtin.debug:
            msg: "SSH key was successfully processed and normalized"

      - name: Verify key normalization (v1.2.0+)
        ansible.builtin.set_fact:
            test_info: "go.ansible v1.2.0+ automatically normalizes SSH keys (CRLF→LF, adds trailing newline)"

      - name: Display test information
        ansible.builtin.debug:
            var: test_info
